<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
    <!-- Border color and width -->
    <stroke
        android:width="@dimen/_1sdp"
        android:color="@color/border_color_unselected" />
    <solid android:color="@color/solid_color_unselected" />

    <!-- Corner radius -->
    <corners android:bottomRightRadius="@dimen/_4sdp"
        android:topRightRadius="@dimen/_4sdp"/>

    <!-- Padding inside the EditText -->
    <padding
        android:right="@dimen/_4sdp"
        android:top="@dimen/_4sdp"
        android:bottom="@dimen/_4sdp" />

</shape>

